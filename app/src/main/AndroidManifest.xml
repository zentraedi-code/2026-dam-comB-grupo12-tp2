<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tp2Dam">

        <!-- Splash: punto de entrada -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Tp2Dam.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Acceso Administrador -->
        <activity
            android:name=".app.ui.auth.login.AdminLoginActivity"
            android:exported="false" />

        <!-- Registro Socio -->
        <activity
            android:name=".app.ui.socio.RegisterSocioActivity"
            android:exported="false" />

        <!-- Registro No Socio -->
        <activity
            android:name=".app.ui.nosocio.RegisterNoSocioActivity"
            android:exported="false" />

    </application>

</manifest>